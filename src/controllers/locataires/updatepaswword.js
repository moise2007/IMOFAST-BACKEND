
const updatePasswordLocataire = (req,res)=>{
    // recupere les donness et le mot de passe

    // 
    res.json({
        sucess: true,
        msg: "requette efectue avec success"
    })
}
module.exports = { updatePasswordLocataire}